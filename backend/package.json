{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "tsc",
    "start": "bun run src/index.ts",
    "migrate": "bun -e \"import { db } from './src/db'; await db.migrate.latest(); console.log('Migrations complete'); process.exit(0)\"",
    "migrate:rollback": "bun -e \"import { db } from './src/db'; await db.migrate.rollback(); console.log('Rollback complete'); process.exit(0)\""
  },
  "dependencies": {
    "@hono/zod-validator": "^0.8.0",
    "@react-set/common": "workspace:*",
    "@react-set/database": "workspace:*",
    "hono": "^4.6.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "typescript": "~6.0.2"
  }
}
